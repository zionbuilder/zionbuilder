export default class {
	constructor (schema, model) {
		this.schema = schema
		this.model = model
	}

	parseSchema (schema, model) {
		return schema
	}

	parseModel (model) {
		return model
	}

	getValue () {

	}
}
